# Frontend Environment Configuration (Template)
# Copy to .env or .env.local for local overrides.
# In local development, defaults to http://127.0.0.1:8000 when unset.

# Production backend API base URL
VITE_API_BASE_URL=https://merchant-finpilot-api.onrender.com
